#!/bin/bash

################################################################################
### 全てのホストで SSH 経由でコマンド実行
################################################################################

if [ $# -eq 0 ]; then
	exit 1
fi

for H in $($(dirname $0)/ALLHOST); do
	echo "### ssh $H $@"
	ssh $H "$@"
done
