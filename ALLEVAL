#!/bin/bash

################################################################################
### 変数 $H にホスト名を設定してコマンド実行
################################################################################

if [ $# -eq 0 ]; then
	exit 1
fi

for H in $($(dirname $0)/ALLHOSTS); do
	echo "# H=$H $@"
	eval $@
done
