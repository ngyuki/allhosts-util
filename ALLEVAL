#!/bin/bash

################################################################################
### 変数 $H にホスト名を設定してコマンド実行
################################################################################

source "$(dirname "$0")"/ALLHOST-functions

if [ $# -eq 0 ]; then
	exit 1
fi

for H in $(gethosts); do
	echo "# H=$H $@"
	eval $@
done
